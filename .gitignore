*.sdf
/UpgradeLog.htm
.vs/WindowTabs/
.claude
.codex
*.wixpdb

# NuGet packages (auto-restored on build)
packages/

# Build artifacts (intermediate files and outputs)
# Note: exe/ folder contains intentional build outputs for distribution
**/obj/
**/bin/Debug/
**/bin/Release/
*.pdb
/exe/

.vscode/
CCXLOG/
CLAUDE*.md

# cc-cx-unite working area (task files, agent logs, measurement screenshots,
# analysis reports). Kept on disk as the record of how a change was arrived at,
# but not part of the shipped source.
unite/

